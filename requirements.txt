# Окружение дисциплины «Структуры и алгоритмы обработки данных», семестр 1
# Python 3.10+. Установка: pip install -r requirements.txt
# Все лабораторные работы выполнимы на CPU; ключевые структуры данных и алгоритмы
# реализуются вручную средствами стандартной библиотеки (см. правила анти-чита в КИМ).

numpy>=1.26,<3          # генерация данных, сверка с эталоном
matplotlib>=3.8,<4      # графики бенчмарков (log-log)

# Работа в ноутбуках (по желанию; сдача допускается в .py)
jupyterlab>=4.0,<5

# Проверки
pytest>=8.0,<9
